## 1. Maven and Offline Dependencies

- [x] 1.1 Add `junit-platform-console-standalone` and `mockito-core` to the root `pom.xml` dependencies to ensure they are available for downloading.
- [x] 1.2 Configure the `maven-dependency-plugin` with the `copy-dependencies` goal in `pom.xml` to export all JARs to `target/dependency/`.
- [x] 1.3 Update `sandbox-runtime/Dockerfile` to `COPY target/dependency/ /app/libs/` and modify `chown` commands if necessary so the sandbox user can read them.

## 2. Sandbox File Preparation

- [x] 2.1 Update `sandbox-runtime/server.js` to detect `mode === 'custom_test'` from the job payload.
- [x] 2.2 If `custom_test`, extract the class name from `harnessCode` (or require it in `sandboxProfile`) and write `harnessCode` to a `<ClassName>.java` file in the isolated temp directory.
- [x] 2.3 Modify the `javac` invocation in `sandbox-runtime/server.js` to compile both the user's `Solution.java` and the newly written Custom Test Harness file together using `-cp ".:/app/libs/*"`.

## 3. Sandbox Test Execution & Parsing

- [x] 3.1 Implement the `raw` runner execution path: invoke `java -cp ".:/app/libs/*" <HarnessClassName>` and parse standard output as the `EvaluationResult` JSON.
- [x] 3.2 Implement the `junit` runner execution path: invoke `java -cp ".:/app/libs/*" org.junit.platform.console.ConsoleLauncher --select-class <HarnessClassName> --reports-dir=reports`.
- [x] 3.3 Add logic to parse the resulting JUnit XML/JSON report generated by Console Launcher, mapping passes/failures to the `EvaluationResult` JSON schema.
- [x] 3.4 Wire the newly parsed outcomes back into the sandbox's HTTP response.

## 4. End-to-End Verification

- [ ] 4.1 Rebuild the sandbox docker image (`docker build`).
- [x] 4.2 Update `challenges.json` to include an example/test challenge using `mode: "custom_test"` and `runner: "junit"` with Mockito injected.
- [ ] 4.3 Verify the execution and scoring in the web UI.
